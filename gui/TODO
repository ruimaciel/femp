- nodal force data structure must inform the programmer if a certain force component was set (i.e., pointer to double)
- implement a single data structure to deal with both nodal displacements and nodal forces, which are mutually exclusive
- import only the elements which are supported, filter out all other cruft
- separate designation for the element type supported by the program and those supported by the document format

- certificar que antes da importação da malha há materiais definidos
- ao importar a malha, escolher o material a atribuir
- seleccionar o tipo de elementos a importar
- não interpretar matrix singular como erro

- apontar DisplayOptions::load_pattern para o objecto LoadPattern referido pela toolbar
- abandonar flex/bison ao escrever um analisador sintático LL para o formato MSH 

- implement Iso viewport angles in DisplacementsViewport
- implement a wireframe surface class for the displacement viewport
- verificar se cargas no domínio fazem referência a um elemento que existe

- visualização de reacções no apoio

== Notas ==
	deslocamentos prescritos podem ter outra orientação além das do referencial
